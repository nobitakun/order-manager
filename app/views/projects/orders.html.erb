<h1><%= @project.name %> 邸の発注書</h1>

<h2>発注書一覧</h2>

<section>
  <%= render 'orders/orders', orders: @project.orders %>
</section>

<section>
  <%= link_to '発注書新規作成', new_order_path(@project.id), class: 'btn btn-lg btn-primary' %>
</section>